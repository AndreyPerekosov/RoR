<%= form_for(@category) do |f| %>
  <%= render 'common/errors', object: @category %>

  <div class="field">
    <%= f.label "Категория" %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
