<h1>Категории</h1>

<table>
  <thead>
    <tr>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.body %></td>
        <%= link_to 'Show', [:admin, category]%> </td> <!-- указываем в явном виде ссылка на наш контроллер -->
        <!-- <td><%= link_to 'Show', category%></td> или можно так(запрос на обычный контроллер), чтобы не писать еще один show для админа -->


        <td><%= link_to 'Edit', edit_admin_category_path(category) %></td> <!-- здесь в явном виде не указывам, т.к. в роутинге есть префикс - используем хэлпер -->
        <td><%= link_to 'Destroy', [:admin, category], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Новая категория', new_admin_category_path %>
